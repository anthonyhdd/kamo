-- APPLIED 2026-08-16 to Supabase project qpztlobbnjyjbxqyuzgg (migration
-- `feed_quality_lqip_reactions`), together with notify-creator v7 (the edge function gains an
-- additive `kind: "reaction"` case; unknown kinds still fall through to the original copy).
--
-- FOUR ADDITIONS AND ONE REORDER, all serving the same night's brief: the feed learns quality,
-- paints instantly, and the reaction loop finally reaches the creator.
--
-- ══════════════════════════════════════════════════════════════════════════════════════
-- 1. LQIP — the placeholder the slide can paint in zero milliseconds.
--
-- A ~20px JPEG as a data URL (~500 bytes), generated by the page at publish from the same
-- board the real JPEG is encoded from, stored on the row, served with every feed page. The
-- slide paints it as a blurred background the instant it mounts, so a swipe on cellular lands
-- on the photo's own colours instead of on black. Nullable forever: every existing hide and
-- every publish from a page loaded before this deploy simply has none, and the slide falls
-- back to today's behaviour.
alter table public.hides add column if not exists lqip text;

-- 2. THE 14-ARGUMENT RUNG of create_hide — the 13-argument body verbatim plus p_lqip,
--    clamped server-side to a data-URL JPEG of bounded length because it arrives from an
--    anonymous client and is about to be injected into strangers' CSS. ADDED, never edited:
--    a page loaded before this migration climbs the shorter rungs untouched.
--    (Body reproduced in the dashboard migration; the only change against the 13-argument
--    rung is v_lqip and the two insert-list entries.)
--
-- 3. FEED_PAGE learns quality and carries the lqip. Both overloads the page calls (2-arg and
--    3-arg) DROPPED AND RECREATED — Postgres cannot widen a set-returning function's row type
--    in place (the get_hide precedent) — atomically inside the migration.
--
--    THE ORDER, and why each key:
--
--      least(n_attempts, 3) asc,          -- distribution first, unchanged: untouched hides
--                                         -- first is what fixed 271-of-2980-ever-opened
--      (reactions + sweet-spot bonus - skip penalty) desc,
--                                         -- QUALITY inside each band, from the three signals
--                                         -- the product only started collecting this week
--      created_at desc                    -- freshness last, so the feed still feels live
--
--    The score, spelled out:
--      + sum(hide_reactions.n)            -- every emoji anyone ever sent it
--      + 3 when n_attempts >= 3 and found-rate is in [25%, 75%]
--                                         -- the difficulty sweet spot: a hide everyone finds
--                                         -- instantly is flat, one nobody finds is
--                                         -- frustration; the bonus rewards real camouflage
--      - least(n_skipped / 3, 4)          -- the give-up counter, /3 because it is young and
--                                         -- noisy (telemetry, not a ledger — its own file
--                                         -- says so) and capped so no pile-on buries a photo
--
--    Pagination is unchanged and inherits the same caveat the band ordering already accepted
--    on 2026-08-13: the cursor stays on created_at, so a page boundary can skip rows whose
--    position moved between fetches. Accepted then for a small room, accepted now.
--
-- 4. FEED_BEST(p_limit, p_block_tags) — the guaranteed strong moment. The best PROVEN hides
--    (5+ attempts) by the same score, band-free. The client seeds ONE of these a few slides
--    into a fresh session, so a new player's first minutes contain at least one photo the
--    room has already voted for. Blocks and shadowbans respected server-side, same argument
--    as feed_page: answering "don't show me this person" with their best photo would be worse
--    than a slow feed.
--
-- 5. MY_REACTIONS(p_ids) — the creator's half of the reaction loop. Same trust model as
--    my_replies: the caller passes the ids it remembers publishing (chMine(), capped at ten),
--    the server answers with per-hide totals. Feeds the mine-grid counters and the wordmark
--    dot's reaction check.
--
-- 6. TRG_NOTIFY_HIDE_REACTION on hide_reactions (insert or update — react_to_hide upserts a
--    counter, so both fire) — a THIRD sibling on notify_hide_reply's exact rails: same
--    private.push_config secret, same one-per-hour throttle read off the hide being reacted
--    to, same 09:00-22:00 local window, same swallow-and-log posture so a notification can
--    never fail a reaction. The throttle is also what keeps 458 reactions/day from becoming
--    458 pushes: one per hide per hour, ceiling.
--
--    The edge function (notify-creator v7) narrows the emoji to the exact four
--    react_to_hide accepts before it reaches a lock screen, and stamps
--    data: { source: "web_notif", key: "reaction", hide } so a future binary can route the
--    tap to the player card — no released binary routes on it yet, so that half ships inert.
--
-- Full DDL lives in the dashboard migration `feed_quality_lqip_reactions`; this file is the
-- record of what changed and why, per the house rule that this directory is the database's
-- only version-controlled history.
--
-- ══════════════════════════════════════════════════════════════════════════════════════
-- FIXED SAME DAY (migration `fix_lqip_regex_repetition`, ~35 minutes after deploy):
--
-- ⚠️ POSTGRES CAPS REGEX REPETITION COUNTS AT 255. The first cut of the 14-argument rung
-- validated the lqip with '{1,6000}' — which is not a generous length cap, it is a syntax
-- error POSTGRES RAISES AT RUNTIME, on every single call. The failure was exactly the shape
-- the ladder is built for: the client's 14-argument form errored, chUpload fell back to the
-- 13-argument rung, every publish succeeded — and lqip stored for nobody, silently. Caught by
-- the post-deploy check (lqip_rows = 0 against fresh publishes), diagnosed with a rolled-back
-- probe, and re-verified live: a valid data URL stores, url(javascript:…) nulls, probes
-- deleted in the same statement.
--
-- The cap lives in length() now, where it should have been:
--   v_lqip := case when p_lqip is not null
--                   and length(p_lqip) <= 6000
--                   and p_lqip ~ '^data:image/jpeg;base64,[A-Za-z0-9+/=]+$'
--              then p_lqip else null end;
--
-- The lesson for the next rung: any bounded quantifier over ~255 in a Postgres regex is a
-- runtime throw, not a validator. Bound with length(), match shape with '+'.
